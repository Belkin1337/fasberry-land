export const CommandLine = ( { children }: { children: React.ReactNode }) => {
  return (
    <span className="underline-offset-8 underline decoration-clone decoration-neutral-700">
      {children}
    </span>
  )
}